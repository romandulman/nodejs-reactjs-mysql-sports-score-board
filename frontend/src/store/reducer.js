const initState = {

};

const reducer = (state = initState, action) => {

    let newState = {...state};
    switch (action.type) {
        case "SHOWMODAL":


            break;

        case "LOGIN":


            break;

        case "IsLoggedIn":

            break;

        case "DATA":

            break;

    }

    return newState
};

export default reducer;